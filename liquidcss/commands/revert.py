import argparse

"""
Command: liquidcss revert

Description:
    Reverts a deploy. Replaces the deployed file with the source.

Positional Arguments: {file_key}
    {file_key} - key to a deployed file.

Optional Arguments:
    [--all -a] - allows skipping of 'file_key' argument and specifies all files.
"""


def revert():
    pass

def main(args):
    pass
        

    
